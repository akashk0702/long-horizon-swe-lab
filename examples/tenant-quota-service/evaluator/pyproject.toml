[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "tenant-quota-evaluator"
version = "0.1.0"
description = "Operator-owned behavioral verification for the tenant quota exercise."
requires-python = ">=3.12,<3.13"
license = "MIT"

[tool.hatch.build.targets.wheel]
packages = ["src/tenant_quota_evaluator"]
